services:
  job-bot:
    build:
      context: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data      # persist SQLite DB
      - ./logs:/app/logs      # persist logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=10).close()"]
      interval: 5m
      timeout: 20s
      retries: 3
      start_period: 10m
